# Position Engine Contract

Her engine aynı interface'i uygular:

```text
validateScope()
classifyRoles()
selectMetrics()
buildBenchmark()
calculatePercentiles()
buildFeatureVector()
applySampleReliability()
runPublicationRules()
```

Engine başka pozisyona ait kritik metrikleri core score'a dahil edemez.
